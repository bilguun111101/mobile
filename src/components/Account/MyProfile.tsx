import { View, Text } from "react-native";
import React, { memo } from "react";

const MyProfile = () => {
  return (
    <View>
      <Text>MyProfile</Text>
    </View>
  );
};

export default memo(MyProfile);
